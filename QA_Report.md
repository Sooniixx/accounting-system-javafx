# Звіт з якості проєкту (QA Report)

**Проєкт:** Інформаційна система бухгалтерії (Варіант 18)  
**СУБД:** H2 Database  
**QA Engineer:** Дерев'янко Дмитро Романович (ІН-31/2)  

## 1. Автоматизоване тестування (JUnit 5)
Для забезпечення надійності системи були розроблені unit-тести. Автотести перевіряють логіку `EmployeeService` та коректність роботи моделі даних `Employee`. 

| Назва тесту                                 | Мета перевірки                                                                           | Очікуваний результат                                                                 | Статус   |
|---------------------------------------------|------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------|----------|
| `testEmployeeModelCreation`                 | Перевірка конструкторів та геттерів/сеттерів моделі `Employee`.                          | Об'єкт успішно створюється, дані ідентичні введеним.                                 | ✅ Passed |
| `testGetAllEmployeesDoesNotCrash`           | Перевірка читання з БД (`SELECT`).                                                       | Запит виконується стабільно, не викидає критичних винятків.                          | ✅ Passed |
| `testAddEmployeeThrowsExceptionOnInvalidData`| Негативний тест. Перевірка реакції СУБД/Сервісу на невалідні дані (неіснуючий відділ).   | Генерується `RuntimeException` з відповідним повідомленням. Запис у БД не потрапляє. | ✅ Passed |

## 2. Ручне тестування інтерфейсу (GUI Testing)
Було проведено тестування JavaFX інтерфейсу (View-Controller) за методикою Black-box на інтегрованій збірці від Team Lead.

| Модуль               | Кроки відтворення                                                                   | Фактичний результат                                                 | Статус   |
|----------------------|-------------------------------------------------------------------------------------|---------------------------------------------------------------------|----------|
| **Create**           | 1. Заповнити всі поля валідними даними.<br>2. Натиснути "Зберегти".                 | Дані з'являються в таблиці GUI та записуються в базу H2.            | ✅ Passed |
| **Read/Search**      | 1. Ввести критерії пошуку (ID відділу / Посада).<br>2. Натиснути кнопку пошуку.     | Таблиця відображає лише відфільтровані результати.                  | ✅ Passed |
| **Update**           | 1. Виділити запис у списку.<br>2. Відредагувати посаду/дні.<br>3. Натиснути "Оновити".| Оновлені дані миттєво відображаються у списку.                      | ✅ Passed |
| **Delete**           | 1. Виділити запис.<br>2. Натиснути "Видалити".                                      | Запис зникає з інтерфейсу і видаляється з БД (`DELETE`).            | ✅ Passed |
| **Валідація**        | 1. Спробувати додати запис без вказання ПІБ.<br>2. Натиснути "Зберегти".            | З'являється вікно з помилкою (Alert). Збереження блокується.        | ✅ Passed |

**Висновок:**  
Програмний продукт повністю відповідає вимогам завдання. Інтеграція Frontend та Backend пройшла успішно, всі CRUD операції та автотести JUnit 5 працюють без помилок.
